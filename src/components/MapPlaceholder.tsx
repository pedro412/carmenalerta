export function MapPlaceholder() {
  return (
    <section
      id="map-root"
      className="map-placeholder"
      aria-label="Espacio reservado para el mapa"
    >
      {/* El componente de mapa se montará dentro de este contenedor. */}
      <span>Espacio reservado para el mapa</span>
    </section>
  )
}
